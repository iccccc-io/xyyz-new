<!--pages/webview/index.wxml-->
<view class="webview-page">
  <nav-bar
    title="{{title}}"
    showTitle="{{true}}"
    showBack="{{true}}"
    background="solid"
    dark="{{true}}"
    placeholder="{{true}}"
  />
  <web-view src="{{url}}" bindload="onLoad" binderror="onError" />
</view>
